<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Version Currency (Rule 42) checked Aug 8 2026 against the live web —
    see Instructor_Material/_verification/BUILD_PLAN.md §4.
    net10.0 matches every other project on this track (current LTS,
    supported to Nov 2028). Microsoft.Data.SqlClient 7.0.2 is the newest
    NON-preview release on nuget.org's flat-container index as of the
    same check (7.1.0 exists only as previews — never adopt a preview
    for teaching, Rule 42).
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>StudentPortalAI</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SqlClient" Version="7.0.2" />
  </ItemGroup>

</Project>
